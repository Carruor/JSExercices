//1. Desarrollar una función que escriba en la consola del navegador cada item de una lista de la compra.


//bucle para recorrer un array
// for (let index = 0^desde donde^; index < array.length ^duracion del recorrido^; index++ ^suma en incremento de 1^) {
    //const element = array[index];
    
let group =["pepe","tomas", "laura","ana"]

function groupList() {
for(var i = 0; i < group. length ; i++){
console. log(group[i])
    }
}
groupList();